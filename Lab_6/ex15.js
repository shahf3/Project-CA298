// Define an array of persons
const persons = ["Alice", "Bob", "Charlie", "David", "Eve"];

// Use a for loop to iterate over the array and print out each person's name
for (let i = 0; i < persons.length; i++) {
  console.log(persons[i]);
}
